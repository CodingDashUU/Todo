@attribute [Route(ApplicationRoutes.Register)]
@inherits NotificationPage

<PageTitle>Register</PageTitle>

<RegistrationCard ReturnUrl="@ReturnUrl"/>

@code {

    [SupplyParameterFromQuery] public string ReturnUrl { get; set; } = "";
}